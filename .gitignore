
!package.json
!templates/.vscode/*.json
!test/golden/*.fasm
# and CI copies site/. wholesale, so these must not reach the published site
# build outputs of a design folder
# keep
# the abandoned Canva import attempt: the shell is coded in site/s.css now,
*.bit
*.fasm
*.frames
*.tgz
*.vcd
*_routed.json
*_sim
.rabadon/
blink.json
dist/
node_modules/
_canva/import/
site/canva.css
site/sim/
top.json

# vercel deploy bundle (deploy.sh)
out/
.vercel/
.fpga_home

# 2409workflow run: plan and script stay local (strategy, not product)
2409workflow.md
.claude/
