
!package.json
!templates/.vscode/tasks.json
!test/golden/*.fasm
# and CI copies site/. wholesale, so these must not reach the published site
# build outputs of a design folder
# keep
# the abandoned Canva import attempt: the shell is coded in site/s.css now,
*.bit
*.fasm
*.frames
*.tgz
*.vcd
*_routed.json
*_sim
.rabadon/
blink.json
dist/
node_modules/
site/_canva/import/
site/canva.css
site/sim/
top.json
